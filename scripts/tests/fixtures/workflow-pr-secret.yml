name: Pull request secret
on: pull_request
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - env:
          TOKEN: ${{ secrets.DEPLOY_TOKEN }}
        run: true
