# ===========================================================================
# InfinityScan — Environment Configuration
# ===========================================================================
# Copy this file to infra/.env and edit as needed.
#
#   cp infra/.env.example infra/.env
#
# All values below are placeholders — replace them before running.
# ===========================================================================

# ---------------------------------------------------------------------------
# Database (PostgreSQL)
# ---------------------------------------------------------------------------

# Username for the Postgres instance.
POSTGRES_USER=changeme_user

# Password for the Postgres instance.
# IMPORTANT: Change this to a strong, unique password in any real environment.
POSTGRES_PASSWORD=changeme_password

# Name of the database to create.
POSTGRES_DB=changeme_db

# Full SQLAlchemy connection URL used by the API and Alembic migrations.
# Format: postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql+psycopg://changeme_user:changeme_password@db:5432/changeme_db

# ---------------------------------------------------------------------------
# API server
# ---------------------------------------------------------------------------

# Host the FastAPI server binds to.
API_HOST=0.0.0.0

# Port the FastAPI server listens on.
API_PORT=8000

# Optional: CopyManga API token (leave blank for anonymous access).
# COPYMANGA_TOKEN=

# Optional: URL prefix for S3 page images (leave blank if unused).
# S3_PUBLIC_URL=

# Optional: Comma-separated list of allowed CORS origins.
# CORS_ORIGINS=http://localhost:3000

# ---------------------------------------------------------------------------
# Web (Next.js)
# ---------------------------------------------------------------------------

# Base URL the browser uses to call the API.
# This is baked into the JS bundle at build time.
# It MUST be a URL reachable by end-user browsers, not a Docker-internal name.
#
# Local dev (without Docker):  http://localhost:8000
# Production:                  https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:8000
